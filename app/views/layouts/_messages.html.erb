<% flash.each do |name, msg| %>
  <div class="alert alert-<%= "#{name}" %> alert-dismissible fade show rounded-0 mb-0" role="alert">
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    <strong><%= msg %></strong> 
  </div>
<% end %>